function solution(s) {
    var answer = '';
    for(var i=0;i<s.length;i++){
        if(s.indexOf(s[i]) == s.lastIndexOf(s[i])){
            answer+=s[i]
        }
    }
    return [...answer].sort().join('');
}