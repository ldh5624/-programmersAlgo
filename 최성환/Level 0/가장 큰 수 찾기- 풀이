function solution(array) {
    var answer = [];
    var array_temp = array.filter(()=>true);
    var test = array.sort(function(a,b){
        return b-a;
    });
    answer.push(test[0])
    answer.push(array_temp.indexOf(test[0]))
    return answer;
}