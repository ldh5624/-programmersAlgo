class Solution {
    public int solution(int[] numbers, int k) {
        int answer = 0;

        int idx = (k-1)*2 +1;
        
        answer = numbers[(idx%numbers.length)-1];
        
        return answer;
    }
}
