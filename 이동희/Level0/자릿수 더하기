class Solution {
    public int solution(int n) {
        int answer = 0;
        
     String str = Integer.toString(n);
	        
	        String a[] = str.split("");
	        
	        
	        for(int i=0;i<a.length;i++) {
	        	answer += Integer.parseInt(a[i]);
	        }
        return answer;
    }
}


-note

Stinrg -> int    Integer.parseInt()
int -> String    Integer.toString()
